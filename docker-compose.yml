services:
  zktaccess:
    build:
      context: .
      args:
        INSTALL_DEV: "true"
    platform: linux/amd64
    container_name: zktaccess
    restart: 'no'
    volumes:
      - ./.env:/app/.env
      - ./pyproject.toml:/app/pyproject.toml
      - ./src:/app/src
      - ./tests:/app/tests

